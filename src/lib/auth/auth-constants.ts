/** Minimum password length for register and reset-password flows. */
export const MIN_PASSWORD_LENGTH = 8;

/** Shown after forgot-password submit (avoids email enumeration). */
export const PASSWORD_RESET_EMAIL_SENT_MESSAGE =
  "If an account exists for that email, we sent a password reset link.";

/** Shown after in-profile password change succeeds. */
export const CHANGE_PASSWORD_SUCCESS_MESSAGE =
  "Password updated. You are still signed in on this device.";
