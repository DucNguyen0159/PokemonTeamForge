import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function StrategiesPage() {
  return (
    <PlaceholderPage
      eyebrow="Strategy teams"
      title="Preset archetypes"
      description="Browse rain, sun, trick room, hyper offense, and other starting points. You&apos;ll be able to filter by format and difficulty, then load a preset into the builder."
    />
  );
}
